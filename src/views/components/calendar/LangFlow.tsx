import React, { useState } from 'react';
import LangflowBot from '@views/components/calendar/LangflowBot';

function LangflowClient() {
    return (
        <div className="LangflowClient">
            <LangflowBot />
        </div>
    );
}

export default LangflowClient;